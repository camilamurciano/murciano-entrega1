{/*
const express = require('express');
const router = require('./router')

const app = express();

//esto ayuda a que el postman lea el json
app.use(express.json())

router(app)

module.exports = app;
*/} 
//anulado por el server.config.js